apiVersion: v1
kind: Service
metadata:
  name: {{ include "myflix.fullname" . }}
  labels:
    {{- include "myflix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myflix.selectorLabels" . | nindent 4 }}
