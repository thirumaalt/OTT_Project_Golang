apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-service
  namespace: myflix
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recommendation-service
  template:
    metadata:
      labels:
        app: recommendation-service
    spec:
      containers:
        - name: recommendation-service
          image: thiru98/recommendation-service:latest
          ports:
            - containerPort: 8087
          env:
            - name: PORT
              value: "8087"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: jwt-secret
          envFrom:
            - configMapRef:
                name: app-config
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8087
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: myflix
spec:
  selector:
    app: recommendation-service
  ports:
    - port: 8087
      targetPort: 8087
